The file root.tex is part of the Isabelle's document 
preparation process. The document for the theories
can be produced using

isabelle build -D .

in the parent directory. In Windows this command should
be run from the Isabelle's Cygwin terminal from the 
Isabelle's installation directory.